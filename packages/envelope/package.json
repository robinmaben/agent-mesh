{
  "name": "@agent-mesh/envelope",
  "version": "0.1.0",
  "description": "AgentMessage envelope schema and types",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
